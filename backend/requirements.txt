# Core
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0.0

# RAG Pipeline
chromadb>=0.5.0
rank-bm25>=0.2.2
sentence-transformers>=3.0.0
pyvi>=0.1.1            # Vietnamese word segmentation for BM25 tokenisation

# LLM Providers
google-genai>=1.0.0
ollama>=0.3.0
# Multimodal (CLIP)
open-clip-torch>=2.24.0
Pillow>=10.0.0

# Re-ranking
# Cap <5: transformers 5.x breaks sentence-transformers 5.x ("Could not import
# module 'PreTrainedModel'"). 4.4x is the compatible line.
transformers>=4.41.0,<5.0
torch>=2.0.0

# Evaluation
ragas==0.4.3
datasets>=4.0.0
# ragas 0.4.3 declares langchain* unpinned; pip otherwise pulls langchain 1.x
# whose langchain-community 0.4.x breaks ragas import (vertexai ModuleNotFound).
# ragas 0.4.3 predates langchain 1.0 — keep the 0.3.x line.
langchain<1.0
langchain-core<1.0
langchain-community<0.4
langchain-openai<1.0

# Utilities
numpy>=2.0.0,<3.0
pandas>=2.0.0,<3.0
matplotlib>=3.8.0       # experiment result plots (plot_results.py)
pyyaml>=6.0
httpx>=0.27.0
psutil>=5.9.0

# Test
pytest>=7.4.0